# Infra

TradeGraph India now runs on a small Node + Vercel-compatible runtime.

This folder holds deployment-oriented configuration such as:

- Vercel routing and cron configuration
- environment expectations for source sync and checkout callbacks
- future infrastructure notes for moving from local JSON persistence to hosted Postgres by default
